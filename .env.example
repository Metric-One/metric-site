# URL of the AdMetrics dashboard app (Start free / Log in / Open dashboard targets).
# Dev default is the dashboard's local Vite server. In prod set this to the
# dashboard's .vercel.app URL. Vite only exposes vars prefixed with VITE_.
VITE_APP_URL=http://localhost:5173
